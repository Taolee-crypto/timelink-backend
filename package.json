{
  "name": "timelink-backend",
  "version": "1.0.0",
  "description": "Timelink Cloudflare Worker backend",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "npx wrangler dev",
    "deploy": "npx wrangler deploy"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "@cloudflare/workers-types": "^4.20251202.0"
  }
}
