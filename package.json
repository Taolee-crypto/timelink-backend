{
  "name": "timelink-backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy",
    "deploy:prod": "wrangler deploy --env production",
    "tail": "wrangler tail",
    "test:api": "curl https://www.timelink.digital/api/health"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
