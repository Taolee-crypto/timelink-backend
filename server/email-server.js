const express=require('express');const app=express();app.use(require('express').json());app.use((req,res,next)=>{res.header('Access-Control-Allow-Origin','*');next();});app.post('/api/email/send-verification',(req,res)=>{console.log('📧 이메일:',req.body.email,'🔐 코드:',req.body.code);res.json({success:true,code:req.body.code,devMode:true});});app.get('/',(req,res)=>res.json({status:'ok'}));app.listen(3001,()=>console.log('✅ 서버: http://localhost:3001'));
